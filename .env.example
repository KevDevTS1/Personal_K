# Copia: cp .env.example .env  y rellena las claves (nunca commitear .env)
# Puerto HTTP
# PORT=8787

# ─── Groq (recomendada) — argumentos largos de cada pick
# https://console.groq.com/keys
GROQ_API_KEY=
# GROQ_MODEL_PRIMARY=llama-3.3-70b-versatile
# GROQ_MODEL_FALLBACK=llama-3.1-8b-instant

# ─── The Odds API (secundaria; consume cuota rápida en plan free)
# https://the-odds-api.com
ODDS_API_KEY=
# false = desactiva llamadas; true = limita a ODDS_API_MAX_SPORTS
ODDS_API_ENABLED=false
ODDS_API_MAX_SPORTS=4

# ─── Clima (OpenWeatherMap; wttr.in es fallback sin key)
# https://openweathermap.org/api
OPENWEATHER_API_KEY=

# ─── football-data.org — tablas, detalle de partido (ligas top)
# https://www.football-data.org/
FOOTBALLDATA_API_KEY=

# ─── TheSportsDB — forma reciente, metadatos (vacío = key pública "3")
# https://www.thesportsdb.com/api.php
THESPORTSDB_KEY=

# ─── API-Football (RapidAPI) — ligas MLS/LATAM, etc. (100 req/día free)
# Misma clave en RapidAPI que para dashboard api-football; si solo tienes
# RAPIDAPI_KEY en otro servicio, puedes usarla con API_FOOTBALL_KEY= o RAPIDAPI_KEY= (ver apifootball.js)
API_FOOTBALL_KEY=
# RAPIDAPI_KEY=
# Tope diario de solicitudes HTTP (por defecto 90; sube si tu plan permite más)
# API_FOOTBALL_MAX_REQUESTS_PER_DAY=90
# Mínimo de cupos libres antes de pedir datos de un partido (local+visitante; peor caso ~4 req)
# API_FOOTBALL_RESERVE_REQUESTS_PER_MATCH=4
# Archivo JSON donde persiste el contador (sobrevive reinicios del servidor)
# API_FOOTBALL_QUOTA_FILE=data/.api-football-quota.json

# Máximo de picks por ciclo con argumento largo Groq/cache (resto usa plantilla; evita timeouts)
# PICKS_LONG_ARGS_CAP=72

# ─── balldontlie (opcional) — NBA stats; sin key = tier gratuito básico
# BALLDONTLIE_API_KEY=

# ─── SofaScore (alineaciones web) — en Railway/Vercel etc. déjalo sin poner:
# la API devuelve 403 a IPs de datacenter (Cloudflare). true = forzar intentos
# (solo si tienes salida por red que no bloquee).
# SOFASCORE_ENABLED=false
